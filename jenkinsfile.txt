node("docker") {
    docker.withRegistry('<<https://hub.docker.com/r/rockabhi58/demo_devops/>>', '<<rockabhi58>>') {
    
        git url: "<<https://github.com/birajdarabhishek1/dockerpipeline.git>>", credentialsId: '<<birajdarabhishek1@gmail.com>>'
    
        sh "git rev-parse HEAD > .git/commit-id"
        def commit_id = readFile('.git/commit-id').trim()
        println commit_id
    
        stage "build"
        def app = docker.build "dockerpipeline"
    
        stage "publish"
        app.push 'master'
        app.push "${commit_id}"
    }
}